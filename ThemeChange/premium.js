let normalColor = ["red", "green", "purple", "white"]
let vipColor = ["gold", "silver", "brown"]

function checkColor() {
        //check premium account
        array.forEach(normalColor => {
                /*
                        if (inputColor === normalColor[) {

                        }
                */
        });
}